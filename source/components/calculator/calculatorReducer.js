import React from "react";

export default function buttonReducer(subArray, action) {
    switch (action.type){
        case "exponent" : {

            return 
        }
        case "multiply" : {
            
            return 
        }
        case "divide" : {
            
            return 
        }
        case "add" : {
            
            return 
        }
        case "subtract" : {
            
            return 
        }
    }
}